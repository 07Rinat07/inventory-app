<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">

    <title>{% block title %}Inventory App{% endblock %}</title>

    {# Bootstrap 5 CSS #}
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
    >

    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark navbar-expand-lg mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('inventories_index') }}">
            Inventory App
        </a>

        <div class="d-flex text-white">
            {% if app.user %}
                <span class="me-3">
                    {{ app.user.email }}
                    {% if app.user.isAdmin %}
                        <span class="badge bg-danger ms-1">ADMIN</span>
                    {% else %}
                        <span class="badge bg-secondary ms-1">USER</span>
                    {% endif %}
                </span>

                <a href="{{ path('app_logout') }}" class="btn btn-outline-light btn-sm">
                    Logout
                </a>
            {% else %}
                <a href="{{ path('login') }}" class="btn btn-outline-light btn-sm">
                    Login
                </a>
            {% endif %}
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>

{# Bootstrap JS (optional, но можно оставить) #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
