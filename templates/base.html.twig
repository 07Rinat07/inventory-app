<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Inventory App{% endblock %}</title>

    {# Bootstrap 5 CDN — только CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('inventories_index') }}">Inventory App</a>

        <div class="ms-auto text-white">
            {% if app.user %}
                <span class="me-3">{{ app.user.email }}</span>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-light btn-sm">
                    Logout
                </a>
            {% else %}
                <a href="{{ path('login') }}" class="btn btn-outline-light btn-sm">
                    Login
                </a>
            {% endif %}
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>

</body>
</html>
