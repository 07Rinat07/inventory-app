{% extends 'base.html.twig' %}

{% block title %}Inventory {{ inventory.title }}{% endblock %}

{% block body %}
    <h1 class="mb-3">{{ inventory.title }}</h1>

    <div class="card mb-4">
        <div class="card-body">
            <p><strong>Category:</strong> {{ inventory.category }}</p>
            <p><strong>Description:</strong> {{ inventory.description }}</p>
            <p><strong>Owner:</strong> {{ inventory.owner.email }}</p>
            <p>
                <strong>Public:</strong>
                {% if inventory.isPublic %}
                    <span class="badge bg-success">Yes</span>
                {% else %}
                    <span class="badge bg-secondary">No</span>
                {% endif %}
            </p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="alert alert-info">
                <strong>Items</strong><br>
                CRUD + Autosave (implemented backend)
            </div>
        </div>

        <div class="col-md-6">
            <div class="alert alert-warning">
                <strong>Discussion</strong><br>
                Polling + Likes
            </div>
        </div>
    </div>

    <hr>

    <p class="text-muted">
        UI is intentionally minimal.<br>
        Business logic, ACL, autosave, polling and likes are implemented on backend level.
    </p>
{% endblock %}
