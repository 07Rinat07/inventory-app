{% extends 'base.html.twig' %}

{% block title %}Inventory {{ inventory.title }}{% endblock %}

{% block body %}
    <h1>{{ inventory.title }}</h1>

    <p><strong>Category:</strong> {{ inventory.category }}</p>
    <p><strong>Description:</strong> {{ inventory.description }}</p>
    <p><strong>Owner:</strong> {{ inventory.owner.email }}</p>

    <hr>

    <h2>Access control</h2>

    {% if app.user and app.user == inventory.owner %}
        <p><strong>You are the owner of this inventory.</strong></p>
    {% elseif app.user and app.user.isAdmin %}
        <p><strong>You are an admin.</strong></p>
    {% else %}
        <p><strong>You are a regular user.</strong></p>
    {% endif %}

    <hr>

    <h2>Sections</h2>
    <ul>
        <li>Items</li>
        <li>Discussion</li>
        <li>Likes</li>
        <li>ACL (via Voters)</li>
    </ul>

    <hr>

    <p style="color: gray;">
        UI is intentionally minimal.
        Access control is enforced in voters and controllers.
    </p>
{% endblock %}
