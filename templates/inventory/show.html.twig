{% extends 'base.html.twig' %}

{% block title %}Inventory {{ inventory.title }}{% endblock %}

{% block body %}
    <h1>{{ inventory.title }}</h1>

    <p><strong>Category:</strong> {{ inventory.category }}</p>
    <p><strong>Description:</strong> {{ inventory.description }}</p>
    <p><strong>Owner:</strong> {{ inventory.owner.email }}</p>

    <hr>

    <h2>Sections</h2>

    <ul>
        <li><strong>Items</strong> (implemented)</li>
        <li><strong>Discussion</strong> (polling)</li>
        <li><strong>Likes</strong></li>
        <li><strong>ACL</strong></li>
    </ul>

    <hr>

    <p>
        UI is intentionally minimal.<br>
        Core logic is implemented in controllers, services, repositories and voters.
    </p>
{% endblock %}
